# Securivon — environment variable template.
#
# Copy this file to .env.local and fill in real values there.
# .env.local is gitignored and must never be committed.
# This file (.env.example) contains placeholders only — no real
# credentials, secrets, or connection strings belong here.
#
# See README.md "Environment variables" for the authoritative
# description of each variable.

# --- Currently read by application code -------------------------------

# PostgreSQL connection string. Required by Prisma (CLI, via
# prisma.config.ts) and by the Prisma driver adapter at runtime
# (src/server/db/client.ts).
DATABASE_URL=

# Secret used to sign Admin session JWTs (jose, HS256). Generate with:
#   openssl rand -base64 32
AUTH_SECRET=

# Canonical production origin (no trailing slash), used to resolve the
# relative canonical/OpenGraph URLs already set on every public page
# (e.g. `alternates: { canonical: "/products" }`) into absolute URLs via
# metadataBase (src/app/layout.tsx), and to build sitemap.xml/robots.txt.
# Falls back to https://securivon.com (the real production domain, per
# README) if unset, so this is optional for local dev.
NEXT_PUBLIC_SITE_URL=https://securivon.com

# Break-glass fallback ONLY — NOT used by normal production login.
# Every real admin login is a database lookup (AdminUser table via Prisma),
# managed through the admin UI itself (see src/app/admin/(dashboard)/account
# for self-service password change). These two vars back a separate,
# currently-inert EnvBootstrapAdminUserRepository class
# (src/server/repositories/adminUserRepository.ts) that nothing in the app
# currently calls — safe to leave both unset in production. Only relevant
# if you later make a deliberate decision to wire in a break-glass login
# path (e.g. for recovering access if the AdminUser table is ever empty or
# the database is unreachable). ADMIN_BOOTSTRAP_PASSWORD_HASH must be a
# bcrypt hash, never a plain-text password, if you do set it.
ADMIN_BOOTSTRAP_EMAIL=
ADMIN_BOOTSTRAP_PASSWORD_HASH=

# --- Documented but not yet wired into application code ---------------
# (see README "Environment variables" — listed here for forward
# compatibility; safe to leave blank until the corresponding feature
# is implemented)

# wa.me link generation. Config value, not a secret.
WHATSAPP_BUSINESS_NUMBER=

# Error tracking. Vendor not yet decided.
ERROR_MONITORING_DSN=

# Analytics. Tool not yet decided.
ANALYTICS_ID=

# Public form spam/bot protection. Not wired in yet at foundation stage.
SPAM_PROTECTION_KEY=

# Product photography / image storage. Exact variable name(s) not yet
# finalized — needed once real, verified product images exist.
# IMAGE_STORAGE_*=
