export default function QuotesPage() {
  return (
    <div>
      <h1 style={{ fontSize: 20, fontWeight: 600, color: "#0F172A", marginBottom: 6 }}>Quotes</h1>
      <p style={{ fontSize: 13, color: "#64748B", maxWidth: 640 }}>
        Not built out this stage — navigation placeholder only. The data model (Quote, QuoteItem,
        with immutability enforced in src/server/quotes/immutability.ts) already exists.
      </p>
    </div>
  );
}
