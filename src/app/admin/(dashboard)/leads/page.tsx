export default function LeadsPage() {
  return (
    <div>
      <h1 style={{ fontSize: 20, fontWeight: 600, color: "#0F172A", marginBottom: 6 }}>Leads</h1>
      <p style={{ fontSize: 13, color: "#64748B", maxWidth: 640 }}>
        Not built out this stage — navigation placeholder only, per the instruction not to build
        future modules fully yet. The data model (Lead, Customer) already exists in
        prisma/schema.prisma.
      </p>
    </div>
  );
}
