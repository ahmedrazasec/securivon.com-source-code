export default function SiteSurveysPage() {
  return (
    <div>
      <h1 style={{ fontSize: 20, fontWeight: 600, color: "#0F172A", marginBottom: 6 }}>Site Surveys</h1>
      <p style={{ fontSize: 13, color: "#64748B", maxWidth: 640 }}>
        Not built out this stage — navigation placeholder only. The data model
        (SiteSurveyRequest) and validation (src/server/validation/schemas.ts) already exist.
      </p>
    </div>
  );
}
